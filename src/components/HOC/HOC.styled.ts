import styled from "styled-components";
import { BaseButton } from "../../atoms/Buttons";

export const SecurityButton = styled(BaseButton)`
  position: absolute;
  right: 0;
  padding: 14px;
  top: 50%;
  transform: translateY(-50%);
`;
